#include <algorithm>
#include <cctype>
#include <cstdio>
#include <cstring>
#include <cmath>
typedef long long ll;
const int MAXN = 200010;
const int MOD = 10007;
struct node {
	int edge[11];
}a[MAXN];
int n, u, v;
int main() {
	scanf("%d", &n);
	for (int i = 1; i <= n; ++i) {
		scanf("%d%d", &u, &v);
	}
}
